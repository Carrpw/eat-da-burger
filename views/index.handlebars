<img src="burger-icon.jpg" alt="burger icon" class="row col-md-12 text-center">

<div class="row col-md-12 text-center">
  <h1>Eat-Da-Burger!</h1>
</div>


<div class="row">
  <div class="col-md-6 text-center">
    <h2>Burgers waiting to be Devoured!</h2>
      <ul>
        {{#each burgers}}
          {{#unless tried}}
            {{> burgers/burger-block eaten=true}}
          {{/unless}}
        {{/each}}
      </ul>
  </div>
  <div class="col-md-6 text-center">
    <h2>You've already tried these burgers....</h2>
      <ul>
        {{#each burgers}}
          {{#if tried}}
            {{> burgers/burger-block eaten=false}}
          {{/if}}
        {{/each}}
      </ul>
  </div>
</div>

<div class="row col-md-12 text-center">
  <h2>Add a type of Burger.</h2>
  <form class="create-form">

    <div class="form-group">
      <label for="bur">Burger Name:</label>
      <input type="text" id="bur" name="name">
      <input type="radio" name="tried" value="0"> Looks Tasty
    </div>
    <button class="btn btn-primary" type="submit">Submit</button>

    <div class="form-group">
      <label for="tried">Enter a burger you want to eat, then click the "Submit" button.</label><br>
    </div>

  </form>
</div>