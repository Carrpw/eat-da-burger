<li>
	{{name}}

	<button class="change-eaten" data-id="{{id}}" data-newtype="{{eaten}}">
		{{#if eaten}}Try Again!{{else}}Try It!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
